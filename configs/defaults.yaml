# Default configuration for dataset creation and model training
fps: 1.0
resize: 640x360
color_mode: gray
dense_threshold: 0.3
neg_ratio: 1.0
train_split: 0.7
val_split: 0.15
test_split: 0.15
label_scheme: is_transition
temporal_window: 2
dataset_version: "0.1"
